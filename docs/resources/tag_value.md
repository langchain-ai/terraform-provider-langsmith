---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "langsmith_tag_value Resource - langsmith"
subcategory: ""
description: |-
  Manages a value belonging to a workspace-scoped LangSmith resource-tag key. Import with <tag_key_id>/<tag_value_id> or <workspace_id>/<tag_key_id>/<tag_value_id>.
---

# langsmith_tag_value (Resource)

Manages a value belonging to a workspace-scoped LangSmith resource-tag key. Import with `<tag_key_id>/<tag_value_id>` or `<workspace_id>/<tag_key_id>/<tag_value_id>`.

## Example Usage

```terraform
resource "langsmith_tag_key" "environment" {
  workspace_id = "00000000-0000-0000-0000-000000000000"
  key          = "Environment"
}

resource "langsmith_tag_value" "production" {
  workspace_id = langsmith_tag_key.environment.workspace_id
  tag_key_id   = langsmith_tag_key.environment.id
  value        = "production"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `tag_key_id` (String) Parent tag key ID.
- `value` (String) Tag value, unique within its key.

### Optional

- `description` (String) Optional tag value description.
- `workspace_id` (String) LangSmith workspace (tenant) ID that owns this tag value. When unset, the resource uses the workspace configured on the provider block.

### Read-Only

- `created_at` (String) Creation timestamp.
- `id` (String) Tag value ID.
- `updated_at` (String) Last update timestamp.
